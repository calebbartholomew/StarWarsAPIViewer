html
	head
		title Ships R Us

		each scrpt in scripts
			script(type=scrpt.type, src=scrpt.src)

		each styl in styles
			link(rel=styl.rel, type=styl.type, href=styl.href)

	body(id='mount-point', class='container-fluid')!= markup
